import * as React from "react";
import { Card, CardContent, CardHeader } from '@mui/material';

export const StudentDashboard=() => (
    <Card>
        <CardHeader title="Welcome student to your dashboard" />
        <CardContent>Congrats on your internship...</CardContent>
    </Card>
);